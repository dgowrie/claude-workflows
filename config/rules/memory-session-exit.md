# Session Exit: Memory Audit

Moved to the `session-wrapup` skill (`skills/session-wrapup/SKILL.md`, section 1), migrated
2026-09-22 as part of a /doctor context cleanup: this was always-loaded content that only
matters when wrapping up a session.
