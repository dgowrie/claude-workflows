# GitHub API Mechanics

Moved to the `github-api-mechanics` skill (`skills/github-api-mechanics/SKILL.md`), migrated
2026-09-22 as part of a /doctor context cleanup: this was always-loaded content that only
matters when scripting the GitHub API with `gh`.
